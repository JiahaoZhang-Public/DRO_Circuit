# Experiment: Smoke Test

**Date:** 2026-03-26
**Author:** Jiahao Zhang
**Status:** planned

## Goal

Verify the full pipeline (score → build → evaluate) runs end-to-end on the GPU server without errors or OOM.

## Environment

- **Server:** gpuhub-root-rtx4090-48
- **GPU:** RTX 4090 48GB
- **Model:** GPT-2 small
- **Task:** IOI, K=5 corruptions
- **N examples:** 20
- **Edge budgets:** [50, 200]
- **Commit:** TBD

## Exact Commands

```bash
ssh gpuhub-root-rtx4090-48
cd /root/projects && git clone --recursive git@github.com:JiahaoZhang-Public/DRO_Circuit.git
cd DRO_Circuit && git checkout codex/project-codebase
source /root/miniconda3/bin/activate && conda create -n dro python=3.10 -y && conda activate dro
pip install -e .
python -m pytest tests/ -v

python experiments/comprehensive_experiment.py \
    --n_examples 20 --batch_size 10 --device cuda --seed 42 \
    --edge_budgets 50 200 --output_dir outputs/exp00_smoke

ls -la outputs/exp00_smoke/
cat outputs/exp00_smoke/summary.json | python -m json.tool | head -60
```

## Results

### Key metrics

| Method | Budget | Mean Faith | Worst-Group Faith | Per-Example Worst Faith |
|--------|--------|-----------|-------------------|------------------------|
| ERM    | 200    |           |                   |                        |
| DRO-max| 200    |           |                   |                        |

### Checklist

- [ ] All 3 phases complete without errors
- [ ] `scores.pt` exists
- [ ] `scores_per_example.pt` exists
- [ ] `summary.json` has entries for naive, erm_mean, max, local_dro circuits
- [ ] No CUDA OOM

## Analysis

TBD

## Conclusion

TBD

## Follow-up

- [ ] If passes → proceed to Exp 1
- [ ] If OOM → reduce batch_size
- [ ] If import errors → fix vendor submodule setup
