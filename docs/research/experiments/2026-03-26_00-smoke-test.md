# Experiment: Smoke Test

**Date:** 2026-03-26
**Author:** Jiahao Zhang
**Status:** completed

## Goal

Verify the full pipeline (score → build → evaluate) runs end-to-end on the GPU server without errors or OOM.

## Environment

- **Server:** gpuhub-root-rtx4090-48
- **GPU:** RTX 4090 48GB
- **Model:** GPT-2 small
- **Task:** IOI, K=5 corruptions
- **N examples:** 20
- **Edge budgets:** [50, 200]
- **Commit:** b417f30

## Exact Commands

```bash
ssh gpuhub-root-rtx4090-48
cd /root/projects && git clone --recursive git@github.com:JiahaoZhang-Public/DRO_Circuit.git
cd DRO_Circuit && git checkout codex/project-codebase
source /root/miniconda3/bin/activate && conda create -n dro python=3.10 -y && conda activate dro
pip install -e .
python -m pytest tests/ -v

python experiments/comprehensive_experiment.py \
    --n_examples 20 --batch_size 10 --device cuda --seed 42 \
    --edge_budgets 50 200 --output_dir outputs/exp00_smoke
```

## Results

### Timing

- Phase 1 (scoring): 3.4s
- Phase 1b (per-example): 3.8s
- Phase 2 (build 32 circuits): 12.9s
- Phase 3 (evaluate 32 circuits): 29.2s
- **Total: 49.2s**

### Key metrics (logit_diff_loss, lower = more faithful)

| Method    | Budget | S2_IO   | IO_RAND | S_RAND  | S1_RAND | IO_S1   | Worst   | Mean    |
|-----------|--------|---------|---------|---------|---------|---------|---------|---------|
| ERM       | 200    | -0.6211 | -0.5283 | -2.8552 | -1.6198 | -1.9700 | -0.5283 | -1.5189 |
| DRO-max   | 200    | -0.5929 | -0.0506 | -2.9674 | -1.6505 | -1.9468 | -0.0506 | -1.4417 |
| Local DRO | 200    | -0.6127 | -0.1116 | -2.8655 | -1.6413 | -1.9413 | -0.1116 | -1.4345 |

### Checklist

- [x] All 3 phases complete without errors
- [x] `scores.pt` exists
- [x] `scores_per_example.pt` exists
- [x] `summary.json` has entries for naive, erm_mean, max, local_dro circuits
- [x] No CUDA OOM

## Analysis

Pipeline runs correctly. N=20 is too small for meaningful comparison — results are noisy. IO_RAND is the hardest corruption for DRO circuits (least negative = least faithful). ERM surprisingly outperforms DRO-max on worst-group at this small N.

## Conclusion

Full pipeline (Phase 1 → 1b → 2 → 3) works end-to-end on RTX 4090. Ready for Exp 1 with N=200.

## Follow-up

- [x] Passes → proceed to Exp 1
